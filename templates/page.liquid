<div class="max-w-4xl mx-auto px-4 sm:px-6 py-14 space-y-6">
  <h1 class="font-serif text-3xl sm:text-4xl text-[#222222] text-center">{{ page.title }}</h1>
  <div class="prose max-w-none text-sm text-[#4A4A4A] leading-relaxed">
    {{ page.content }}
  </div>
</div>
